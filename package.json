{
  "name": "kyber",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "full": "./run-full.sh",
    "build": "vite build",
    "preview": "vite preview",
    "formato": "prettier --check .",
    "formato:aplicar": "prettier --write .",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up --build -d",
    "docker:dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml --profile dev up --build"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "vite": "^6.3.1"
  }
}
